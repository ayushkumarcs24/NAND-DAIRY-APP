$kind: http-request
name: Get Today's Milk Entries
url: "{{base_url}}/api/milk-entries/today"
method: GET
params:
  - key: shift
    value: morning
    enabled: false
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 1775330100002
