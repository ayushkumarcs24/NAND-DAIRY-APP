$kind: http-request
name: Add Samiti to Vehicle
url: "{{base_url}}/api/vehicles/1/samitis"
method: POST
body:
  type: json
  content: |-
    {
      "samiti_id": 1
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 1775330042374
