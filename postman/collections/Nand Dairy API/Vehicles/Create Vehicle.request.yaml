$kind: http-request
name: Create Vehicle
url: "{{base_url}}/api/vehicles"
method: POST
body:
  type: json
  content: |-
    {
      "vehicle_number": "RJ-14-XY-1234",
      "samiti_id": 1
    }
auth:
  credentials:
    - key: token
      value: "{{token}}"
  type: bearer
order: 1775330042373
